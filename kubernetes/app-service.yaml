apiVersion: v1
kind: Service

metadata:
  name: student-app-service

spec:

  selector:
    app: student-app

  ports:

    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080

  type: NodePort